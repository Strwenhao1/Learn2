<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bjsxt.mapper.EmpMapper">
    <select id="selectAll" resultMap="rm">
        select  * from emp;
    </select>

    <resultMap id="rm" type="emp">
        <id column="empno" property="empNo"></id>
        <association property="dept" column="deptno" select="com.bjsxt.mapper.DeptMapper.selectByNo1" javaType="dept"></association>
    </resultMap>

    <select id="selectByNo" resultType="emp">
        select * from emp where deptno = #{deptNo}
    </select>

</mapper>